%h2= link_to work.title, "/work/#{work.slug}/" 
%time= work.date
%p= work.summary
%h4 Roles
%ul
  - work.roles.each do |role|
    %li= role
%a{:href => work.url} Visit Site
%img{:src => work.desktop_image}
%img{:src => work.mobile_image}
